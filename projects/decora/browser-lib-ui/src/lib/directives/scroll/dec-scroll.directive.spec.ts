import { DecScrollDirective } from './dec-scroll.directive';

describe('DecScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new DecScrollDirective();
    expect(directive).toBeTruthy();
  });
});
