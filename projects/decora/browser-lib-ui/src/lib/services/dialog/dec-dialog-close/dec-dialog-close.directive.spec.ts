import { DecDialogCloseDirective } from './dec-dialog-close.directive';

describe('DecDialogCloseDirective', () => {
  it('should create an instance', () => {
    const directive = new DecDialogCloseDirective();
    expect(directive).toBeTruthy();
  });
});
