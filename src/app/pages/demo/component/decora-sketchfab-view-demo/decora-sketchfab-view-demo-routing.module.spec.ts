import { DecoraSketchfabViewDemoRoutingModule } from './decora-sketchfab-view-demo-routing.module';

describe('DecoraSketchfabViewDemoRoutingModule', () => {
  let decoraSketchfabViewDemoRoutingModule: DecoraSketchfabViewDemoRoutingModule;

  beforeEach(() => {
    decoraSketchfabViewDemoRoutingModule = new DecoraSketchfabViewDemoRoutingModule();
  });

  it('should create an instance', () => {
    expect(decoraSketchfabViewDemoRoutingModule).toBeTruthy();
  });
});
