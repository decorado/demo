import { DecoraImageZoomDemoRoutingModule } from './decora-image-zoom-demo-routing.module';

describe('DecoraImageZoomDemoRoutingModule', () => {
  let decoraImageZoomDemoRoutingModule: DecoraImageZoomDemoRoutingModule;

  beforeEach(() => {
    decoraImageZoomDemoRoutingModule = new DecoraImageZoomDemoRoutingModule();
  });

  it('should create an instance', () => {
    expect(decoraImageZoomDemoRoutingModule).toBeTruthy();
  });
});
