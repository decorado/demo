import { AfterViewInit, EventEmitter, OnDestroy, OnInit, QueryList } from '@angular/core';
import { ActivatedRoute, Router } from '@angular/router';
import { DecTabComponent } from './tab/tab.component';
import { DecTabMenuComponent } from './tab-menu/tab-menu.component';
export declare class DecTabsComponent implements OnInit, AfterViewInit, OnDestroy {
    private route;
    private router;
    tabs: QueryList<DecTabComponent>;
    tabMenuComponent: DecTabMenuComponent;
    hidden: any;
    persist: boolean;
    destroyOnBlur: boolean;
    name: string;
    padding: boolean;
    activeTab: string;
    activeTabChange: EventEmitter<string>;
    readonly activeTabIndex: number;
    readonly activeTabObject: any;
    private _activeTab;
    private _activeTabIndex;
    private _activeTabObject;
    private activatedTabs;
    private queryParamsSubscription;
    private pathFromRoot;
    constructor(route: ActivatedRoute, router: Router);
    ngOnInit(): void;
    ngAfterViewInit(): void;
    ngOnDestroy(): void;
    shouldTabBeDisplayed(tab: any): any;
    onChangeTab($event: any): void;
    parseTotal(total: any): any;
    reset(): void;
    private componentTabName();
    private ensureUniqueName;
    private ensureUniqueTabNames;
    private persistTab(tab);
    private selectTab;
    private startSelectedTab();
    private watchTabInUrlQuery();
    private stopWatchingTabInUrlQuery();
}
