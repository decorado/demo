import { OnInit, EventEmitter, Renderer } from '@angular/core';
export declare class DecProductSpinComponent implements OnInit {
    private renderer;
    frameShown: number;
    scenes: string[];
    loadingImages: boolean;
    placeholderScene: string;
    started: boolean;
    totalImagesLoaded: number;
    loadingImage: string;
    looping: boolean;
    onlyModal: boolean;
    FALLBACK_IMAGE: string;
    startInCenter: boolean;
    showOpenDialogButton: boolean;
    spin: any;
    open: EventEmitter<any>;
    private scenesLen;
    private mouseDown;
    private lastMouseEvent;
    private containerWidth;
    private interval;
    private positionDiff;
    private _spin;
    onDragStart(event: any): boolean;
    onMousedown(event: any): void;
    onMousemove(event: MouseEvent): void;
    constructor(renderer: Renderer);
    ngOnInit(): void;
    markAsLoaded: (event: any) => void;
    goLeft: () => void;
    goRight: () => void;
    start: ($event: any) => void;
    loaderPercentage: () => string | 0;
    onOpen($event: any): void;
    private resetStartPositionBasedOnCompany(spin, scenes);
    private resetScenesData(scenes);
    private loadScenes(spin);
    private calculatePosition(event);
    private getUrlsFromSysFiles(sysFiles);
}
